# StigeSpillet

Start alltid med :
`git pull`

Så bytter du til din brach: `git switch <brach name>`

Når du har gjort endringer: `git add .`

Ligg til en melding om hva du har gjort: `git commit -m "melding"`

For å sende til github: `git push -u origin <branch name>`


